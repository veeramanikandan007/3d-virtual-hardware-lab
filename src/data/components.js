import { COMPONENT_MANIFEST } from './componentManifest';

/**
 * componentsData: Canonical component library containing all 20 categories and 194+ components.
 * Backward-compatible export used across WorkbenchContext, ThreeDCanvas, and search tools.
 */
export const componentsData = COMPONENT_MANIFEST;
